--- AdminReports.jsx
+++ AdminReports.jsx
@@ -1,7 +1,7 @@
 import React, { useState, useEffect } from 'react';
 import { Download, Filter } from 'lucide-react';
-import jsPDF from 'jspdf';
+import { jsPDF } from 'jspdf';
 import 'jspdf-autotable';
 
 const AdminReports = ({ token }) => {
@@ -12,6 +12,13 @@
     messages: 0,
     visits: 0
   });
+
+  const [rawData, setRawData] = useState({
+    users: [],
+    projects: [],
+    blogs: [],
+    messages: []
+  });
   
   const [adminName, setAdminName] = useState('System Administrator');
   const [loading, setLoading] = useState(true);
@@ -46,14 +53,16 @@
         const visitsCount = visitRes.ok ? await visitRes.json() : 0;
         const team = teamRes.ok ? await teamRes.json() : [];
 
-        setMetrics({
-          users: team.length,
-          projects: projects.length,
-          blogs: blogs.length,
-          messages: messages.length,
-          visits: visitsCount
+        setRawData({
+          users: team,
+          projects: projects,
+          blogs: blogs,
+          messages: messages,
+          totalVisits: visitsCount // save raw visits count
         });
 
       } catch (e) {
         console.error("Failed to fetch report data", e);
       } finally {
@@ -62,6 +71,38 @@
     };
     
     fetchData();
   }, [token]);
 
+  useEffect(() => {
+    // Filter logic
+    const filterByDate = (items) => {
+      if (!startDate && !endDate) return items;
+      
+      return items.filter(item => {
+        if (!item.createdAt) return true;
+        
+        const itemDate = new Date(item.createdAt);
+        itemDate.setHours(0, 0, 0, 0); // Normalize to start of day for accurate comparison
+        
+        let isValid = true;
+        if (startDate) {
+          const sDate = new Date(startDate);
+          sDate.setHours(0, 0, 0, 0);
+          if (itemDate < sDate) isValid = false;
+        }
+        if (endDate) {
+          const eDate = new Date(endDate);
+          eDate.setHours(23, 59, 59, 999);
+          if (itemDate > eDate) isValid = false;
+        }
+        return isValid;
+      });
+    };
+
+    setMetrics({
+      users: filterByDate(rawData.users || []).length,
+      projects: filterByDate(rawData.projects || []).length,
+      blogs: filterByDate(rawData.blogs || []).length,
+      messages: filterByDate(rawData.messages || []).length,
+      visits: rawData.totalVisits || 0 // Currently no date-filtering for visits since backend only returns total count
+    });
+  }, [startDate, endDate, rawData]);
+
   const generatePDF = () => {
